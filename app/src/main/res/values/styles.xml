<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/color_barraMenu_dia</item> //esto quita el color morado en la parte superior de la pantalla
    </style>



    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">

        <item name="colorControlNormal">@color/colorBlano</item>
        <item name="titleTextColor">@color/colorBlano</item>

    </style>

</resources>
